@(Html.Kendo().Grid<ExecViewHrk.EfClient.DdlSkillType>()
                            .Name("gridDdlSkillType")
        .Resizable(resize => resize.Columns(true))
        .Filterable()
        .Editable(editable => editable.Mode(GridEditMode.InLine))
        .Columns(columns =>
        {
            columns.Command(command =>
            {
                command.Custom("gridEditCommand").Text("<i class='fa fa-pencil txt-primary f-18'></i>").Click("gridEdit");
                command.Custom("gridDeleteCommand").Text("<i class='fa fa-trash-o m-0 txt-danger f-18'></i>").Click("gridDelete");
            });
            columns.Bound(c => c.SkillTypeId).Visible(false);
            columns.Bound(c => c.Code);
            columns.Bound(c => c.Description);

        })
        .Sortable()
        .Selectable()
        .Pageable(pageable => pageable.Refresh(false).PageSizes(true))
        .Events(e => e.DataBound("gridDataBound"))
        .DataSource(dataSource => dataSource
            .Ajax()
                    .Read(read => read.Action("DdlSkillTypesList_Read", "DdlSkillTypes"))
                    .Create(create => create.Action("DdlSkillTypesList_Create", "DdlSkillTypes"))
                    .Update(update => update.Action("DdlSkillTypesList_Update", "DdlSkillTypes"))
                    .Destroy(destroy => destroy.Action("DdlSkillTypesList_Destroy", "DdlSkillTypes"))
            .Model(model =>
                    {
                        model.Id(p => p.SkillTypeId);
                        model.Field(p => p.SkillTypeId).Editable(false);
                        model.Field(p => p.Code);
                        model.Field(p => p.Description);
                    }
                  )
            .Batch(false)
            .Events(events =>
            {
                events.Error("refreshGrid");
                events.RequestEnd("gridRequestEnd");
            })
        )
)

<script type="text/javascript">
    $(".k-grid-add").click(function () {
        setTimeout(function () {
            $('a[class*="k-grid-update"]').html('<span class="k-icon k-update"></span>Create');
        }, 1)
    });
</script>





