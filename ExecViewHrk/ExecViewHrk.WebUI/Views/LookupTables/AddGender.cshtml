@model ExecViewHrk.EfClient.DdlGender
<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
        <span aria-hidden="true">×</span>
    </button>
    <h4 class="modal-title">Add Gender</h4>
</div>
<div class="modal-body">
    <div class="row">
        <form id="frmGender">
            <div class="col-sm-12">
                <div class="form-group">
                    <label><span style="color:red">*</span>Code</label>
                    @Html.TextBoxFor(x => x.Code, new { @class = "form-control", data_required = "Y" })
                </div>
            </div>
            <div class="col-sm-12">
                <div class="form-group">
                    <label><span style="color:red">*</span>Description</label>
                    @Html.TextBoxFor(x => x.Description, new { @class = "form-control", data_required = "Y" })
                </div>
            </div>
            @Html.HiddenFor(x => x.Active)
        </form>
    </div>
</div>
<div class="modal-footer">
    <button type="button" class="btn btn-default waves-effect m-r-15" data-dismiss="modal" id="btnCloseGender">Close</button>
    <button type="button" class="btn btn-primary waves-effect waves-light " id="btnSaveGender">Save</button>
</div>
<script>
    $("#btnSaveGender").click(function (e) {
        e.preventDefault();

        if (!ValidateForm("frmGender")) {
            loading(false);
            return;
        }

        var postData = $("#frmGender").serializeArray();
        var formURL = $("#ApplicationUrl").val() + "LookupTables/DdlGenderList_Create";
        $.post(formURL, postData, function (data, status) {
            if (data.Errors != null) {
                toastr.error(data.Errors[""].errors[0]);
            }
            else {
                reloadGenderDropDown();
                $("#btnCloseGender").trigger('click');
            }
        });
    });
    function reloadGenderDropDown() {
        var _selectedValue = $("#GenderId").val();
        $.get($("#appBaseUrl").val() + "LookupTables/GetDdlGenderList", function (data, status) {
            var list = data;
            if (list.length > 0) {
                $("#GenderId").empty();
                $("#GenderId").append("<option value=''>Select</option>");
                for (var i = 0; i < list.length; i++) {
                    if (list[i].keyvalue == _selectedValue) {
                        $("#GenderId").append("<option value='" + list[i].GenderId + "' selected >" + list[i].Description + "</option>");
                    } else {
                        $("#GenderId").append("<option value='" + list[i].GenderId + "' >" + list[i].Description + "</option>");
                    }


                }
            }
        });
    }
</script>