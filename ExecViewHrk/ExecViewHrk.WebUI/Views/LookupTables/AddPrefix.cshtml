@model ExecViewHrk.EfClient.DdlPrefix
<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
        <span aria-hidden="true">×</span>
    </button>
    <h4 class="modal-title">Add Prefix</h4>
</div>
<div class="modal-body">
    <div class="row">
        <form id="frmPrefix">
            <div class="col-sm-12">
                <div class="form-group">
                    <label><span style="color:red">*</span>Code</label>
                    @Html.TextBoxFor(x => x.Code, new { @class = "form-control", data_required = "Y" })
                </div>
            </div>
            <div class="col-sm-12">
                <div class="form-group">
                    <label><span style="color:red">*</span>Description</label>
                    @Html.TextBoxFor(x => x.Description, new { @class = "form-control", data_required = "Y" })
                </div>
            </div>
            @Html.HiddenFor(x => x.Active)
        </form>
    </div>
</div>
<div class="modal-footer">
    <button type="button" class="btn btn-default waves-effect m-r-15" data-dismiss="modal" id="btnClosePrefix">Close</button>
    <button type="button" class="btn btn-primary waves-effect waves-light " id="btnSavePrefix">Save</button>
</div>
<script>
    $("#btnSavePrefix").click(function (e) {
        e.preventDefault();

        if (!ValidateForm("frmPrefix")) {
            loading(false);
            return;
        }

        var postData = $("#frmPrefix").serializeArray();
        var formURL = $("#ApplicationUrl").val() + "LookupTables/DdlPrefixList_Create";
        $.post(formURL, postData, function (data, status) {
            if (data.Errors != null) {
                toastr.error(data.Errors[""].errors[0]);
            }
            else {
                reloadPrefixDropDown();
                $("#btnClosePrefix").trigger('click');
            }
        });
    });
    function reloadPrefixDropDown() {
        var _selectedValue = $("#PrefixId").val();
        $.get($("#appBaseUrl").val() + "LookupTables/GetDdlPrefixList", function (data, status) {
            var list = data;
            if (list.length > 0) {
                $("#PrefixId").empty();
                $("#PrefixId").append("<option value=''>Select</option>");
                for (var i = 0; i < list.length; i++) {
                    if (list[i].keyvalue == _selectedValue) {
                        $("#PrefixId").append("<option value='" + list[i].PrefixId + "' selected >" + list[i].Description + "</option>");
                    } else {
                        $("#PrefixId").append("<option value='" + list[i].PrefixId + "' >" + list[i].Description + "</option>");
                    }


                }
            }
        });
    }
</script>