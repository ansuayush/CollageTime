<div class="theme-grid">

    @(Html.Kendo().Grid<ExecViewHrk.EfClient.DdlBusinessLevelTypes>()
            .Name("gridDdlUnion")
        .Resizable(resize => resize.Columns(true))
        .Filterable()
        .Editable(editable => editable.Mode(GridEditMode.InLine))
        .Columns(columns =>
        {
            columns.Command(command =>
            {
                command.Custom("gridEditCommand").Text("<i class='fa fa-pencil txt-primary f-18'></i>").Click("gridEdit");
                command.Custom("gridDeleteCommand").Text("<i class='fa fa-trash-o m-0 txt-danger f-18'></i>").Click("gridDelete");
            }).Width(70);
            columns.Bound(c => c.BusinessLevelTypeNbr).Visible(false);
            columns.Bound(c => c.Code);
            columns.Bound(c => c.Description);//.Title("Description");
            columns.Bound(c => c.Active).ClientTemplate("#= Active ? 'Yes' : 'No' #");
        })
        .Sortable()
        .Selectable()
        .Pageable(pageable => pageable.Refresh(false).PageSizes(true))
        .DataSource(dataSource => dataSource.Ajax()
                    .Read(read => read.Action("DdlUnionsList_Read", "LookupTables"))
                    .Create(create => create.Action("DdlUnions_Create", "LookupTables"))
                    .Update(update => update.Action("DdlUnionsList_Update", "LookupTables"))
                    .Destroy(destroy => destroy.Action("DdlUnions_Destroy", "LookupTables"))
                    .Model(model =>
                    {
                        model.Id(p => p.BusinessLevelTypeNbr);
                        model.Field(p => p.BusinessLevelTypeNbr).Editable(false);
                        model.Field(p => p.Code);
                        model.Field(p => p.Description);
                    }
                    )
            .PageSize(5)
            .Batch(false)
            .Events(events =>
            {
                events.Error("refreshGrid");
                events.RequestEnd("gridRequestEnd");
            })
        )
    )
</div>
<script type="text/javascript">

    $(".k-grid-add").click(function () {
        setTimeout(function () {
            $('a[class*="k-grid-update"]').html('<span class="k-icon k-update"></span>Create');
        }, 1)
    });

</script>



