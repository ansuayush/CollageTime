@model ExecViewHrk.EfClient.DdlProfessionalBody
<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
        <span aria-hidden="true">×</span>
    </button>
    <h4 class="modal-title">Add Professional Body</h4>
</div>
<div class="modal-body">
    <div class="row">
        <form id="frmProfessionalBody">
            <div class="col-sm-12">
                <div class="form-group">
                    <label><span style="color:red">*</span>Code</label>
                    @Html.TextBoxFor(x => x.Code, new { @class = "form-control", data_required = "Y" })
                </div>
            </div>
            <div class="col-sm-12">
                <div class="form-group">
                    <label><span style="color:red">*</span>Description</label>
                    @Html.TextBoxFor(x => x.Description, new { @class = "form-control", data_required = "Y" })
                </div>
            </div>
            @Html.HiddenFor(x => x.Active)
        </form>
    </div>
</div>
<div class="modal-footer">
    <button type="button" class="btn btn-default waves-effect m-r-15" data-dismiss="modal" id="btnCloseProfessionalBody">Close</button>
    <button type="button" class="btn btn-primary waves-effect waves-light " id="btnSaveProfessionalBody">Save</button>
</div>
<script>
    $("#btnSaveProfessionalBody").click(function (e) {
        e.preventDefault();

        if (!ValidateForm("frmProfessionalBody")) {
            loading(false);
            return;
        }

        var postData = $("#frmProfessionalBody").serializeArray();
        var formURL = $("#ApplicationUrl").val() + "DdlProfessionalBodies/DdlProfessionalBodiesList_Create";
        $.post(formURL, postData, function (data, status) {
            if (data.Errors != null) {
                toastr.error(data.Errors[""].errors[0]);
            }
            else {
                reloadProfessionalBodyDropDown();
                $("#btnCloseProfessionalBody").trigger('click');
            }
        });
    });
    function reloadProfessionalBodyDropDown() {
        var _selectedValue = $("#ProfessionalBodyId").val();
        $.get($("#appBaseUrl").val() + "DdlProfessionalBodies/GetProfessionalBodies", function (data, status) {
            var list = data;
            if (list.length > 0) {
                $("#ProfessionalBodyId").empty();
                $("#ProfessionalBodyId").append("<option value=''>Select</option>");
                for (var i = 0; i < list.length; i++) {
                    if (list[i].keyvalue == _selectedValue) {
                        $("#ProfessionalBodyId").append("<option value='" + list[i].ProfessionalBodyId + "' selected >" + list[i].Description + "</option>");
                    } else {
                        $("#ProfessionalBodyId").append("<option value='" + list[i].ProfessionalBodyId + "' >" + list[i].Description + "</option>");
                    }


                }
            }
        });
    }
</script>