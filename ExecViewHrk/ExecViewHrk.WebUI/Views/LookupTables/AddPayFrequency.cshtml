@model ExecViewHrk.EfClient.DdlPayFrequency
<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
        <span aria-hidden="true">×</span>
    </button>
    <h4 class="modal-title">Add Pay Frequency</h4>
</div>
<div class="modal-body">
    <div class="row">
        <form id="frmPayFrequency">
            <div class="col-sm-12">
                <div class="form-group">
                    <label><span style="color:red">*</span>Code</label>
                    @Html.TextBoxFor(x => x.Code, new { @class = "form-control", data_required = "Y" })
                </div>
            </div>
            <div class="col-sm-12">
                <div class="form-group">
                    <label><span style="color:red">*</span>Description</label>
                    @Html.TextBoxFor(x => x.Description, new { @class = "form-control", data_required = "Y" })
                </div>
            </div>
            @Html.HiddenFor(x => x.Active)
        </form>
    </div>
</div>
<div class="modal-footer">
    <button type="button" class="btn btn-default waves-effect m-r-15" data-dismiss="modal" id="btnClosePayFrequency">Close</button>
    <button type="button" class="btn btn-primary waves-effect waves-light " id="btnSavePayFrequency">Save</button>
</div>
<script>
    $("#btnSavePayFrequency").click(function (e) {
        e.preventDefault();

        if (!ValidateForm("frmPayFrequency")) {
            loading(false);
            return;
        }

        var postData = $("#frmPayFrequency").serializeArray();
        var formURL = $("#ApplicationUrl").val() + "DdlPayFrequencies/DdlPayFrequenciesList_Create";
        $.post(formURL, postData, function (data, status) {
            if (data.Errors != null) {
                toastr.error(data.Errors[""].errors[0]);
            }
            else {
                reloadPayFrequencyDropDown();
                $("#btnClosePayFrequency").trigger('click');
            }
        });
    });
    function reloadPayFrequencyDropDown() {
        var _selectedValue = $("#PayFrequencyId").val();
        $.get($("#appBaseUrl").val() + "DdlPayFrequencies/GetPayFrequencies", function (data, status) {
            var list = data;
            if (list.length > 0) {
                $("#PayFrequencyId").empty();
                $("#PayFrequencyId").append("<option value=''>Select</option>");
                for (var i = 0; i < list.length; i++) {
                    if (list[i].keyvalue == _selectedValue) {
                        $("#PayFrequencyId").append("<option value='" + list[i].Code + "' selected >" + list[i].Description + "</option>");
                    } else {
                        $("#PayFrequencyId").append("<option value='" + list[i].Code + "' >" + list[i].Description + "</option>");
                    }


                }
            }
        });
    }
</script>