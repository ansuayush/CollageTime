@model ExecViewHrk.EfClient.DdlEvaluationTest
<div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
        <span aria-hidden="true">×</span>
    </button>
    <h4 class="modal-title">Add Evaluation Test</h4>
</div>
<div class="modal-body">
    <div class="row">
        <form id="frmEvaluationTest">
            <div class="col-sm-12">
                <div class="form-group">
                    <label><span style="color:red">*</span>Code</label>
                    @Html.TextBoxFor(x => x.Code, new { @class = "form-control", data_required = "Y" })
                </div>
            </div>
            <div class="col-sm-12">
                <div class="form-group">
                    <label><span style="color:red">*</span>Description</label>
                    @Html.TextBoxFor(x => x.Description, new { @class = "form-control", data_required = "Y" })
                </div>
            </div>
            @Html.HiddenFor(x => x.Active)
        </form>
    </div>
</div>
<div class="modal-footer">
    <button type="button" class="btn btn-default waves-effect m-r-15" data-dismiss="modal" id="btnCloseEvaluationTest">Close</button>
    <button type="button" class="btn btn-primary waves-effect waves-light " id="btnSaveEvaluationTest">Save</button>
</div>
<script>
    $("#btnSaveEvaluationTest").click(function (e) {
        e.preventDefault();

        if (!ValidateForm("frmEvaluationTest")) {
            loading(false);
            return;
        }

        var postData = $("#frmEvaluationTest").serializeArray();
        var formURL = $("#ApplicationUrl").val() + "DdlEvaluationTests/DdlEvaluationTestsList_Create";
        $.post(formURL, postData, function (data, status) {
            if (data.Errors != null) {
                toastr.error(data.Errors[""].errors[0]);
            }
            else {
                reloadEvaluationTestDropDown();
                $("#btnCloseEvaluationTest").trigger('click');
            }
        });
    });
    function reloadEvaluationTestDropDown() {
        var _selectedValue = $("#EvaluationTestId").val();
        $.get($("#appBaseUrl").val() + "DdlEvaluationTests/GetEvaluationTests", function (data, status) {
            var list = data;
            if (list.length > 0) {
                $("#EvaluationTestId").empty();
                $("#EvaluationTestId").append("<option value=''>Select</option>");
                for (var i = 0; i < list.length; i++) {
                    if (list[i].keyvalue == _selectedValue) {
                        $("#EvaluationTestId").append("<option value='" + list[i].EvaluationTestId + "' selected >" + list[i].Description + "</option>");
                    } else {
                        $("#EvaluationTestId").append("<option value='" + list[i].EvaluationTestId + "' >" + list[i].Description + "</option>");
                    }


                }
            }
        });
    }
</script>